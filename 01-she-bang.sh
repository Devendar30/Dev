#!/bin/bash

#! is called as she-bang,
# 1. It is used to denote the path of interpreter (Interpreter is the one which executes the given order), &
#    executes all the commands in the file/script using that interpreter.
#     Adding more she-bangs in next lines will not have nay impact.

# All the above lines starting with # are the comments
# 1. comments are used for adding some description for the code, like why you added that, for future reference.
# 2. commented lines are ignored by the interpreters.